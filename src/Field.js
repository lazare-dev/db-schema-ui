import React from 'react';

function Field({ field }) {
  return (
    <div>
      <p>Field: {field.name}</p>
      {/* Implement field configuration options */}
    </div>
  );
}

export default Field;
